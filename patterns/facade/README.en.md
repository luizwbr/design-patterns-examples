# Facade Pattern

This example demonstrates the Facade pattern using a computer startup system.

## Description

The Facade pattern provides a simplified interface to a complex subsystem. In this example, `ComputerFacade` simplifies the complex process of starting a computer by coordinating CPU, Memory, and HardDrive subsystems.
