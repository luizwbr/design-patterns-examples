# Iterator Pattern

Demonstrates the Iterator pattern using a collection iterator.

## Description

The Iterator pattern provides a way to access the elements of an aggregate object sequentially without exposing its underlying representation. In this example, we create a collection of items and provide an iterator to traverse through them.