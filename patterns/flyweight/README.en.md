# Flyweight Pattern

Demonstrates the Flyweight pattern using character formatting in a text editor.

## Description

The Flyweight pattern reduces memory usage by sharing common parts of objects. In this example, we represent characters in a text editor where each character shares formatting information (font, size, color) to minimize memory consumption.